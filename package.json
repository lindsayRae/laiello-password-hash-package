{
  "name": "laiello-password-hash-package",
  "version": "1.0.0",
  "description": "A suite of functions to hash and validate passwords.",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "release": "babel src -d lib && npm version patch && cpx package.json lib/ && cd lib && npm publish",
    "start": "npm run build && node lib/index",
    "doc": "jsdoc src -d docs",
    "test": "npm run start && jest"
  },
  "keywords": [],
  "author": "lindsay.aiellodev@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "jest": "^26.4.2",
    "jsdoc": "^3.6.6"
  },
  "dependencies": {
    "cpx": "^1.5.0"
  }
}
